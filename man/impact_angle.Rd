% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shooting.R
\name{impact_angle}
\alias{impact_angle}
\title{Impact angle from a bullet defect's ellipse}
\usage{
impact_angle(width, length, se = NULL, n_sim = 10000, level = 0.95)
}
\arguments{
\item{width, length}{Numeric. Minor and major axis of the defect, same units.}

\item{se}{Optional numeric. Measurement standard error for both axes. If
supplied, a Monte Carlo interval is added (\code{n_sim} draws).}

\item{n_sim}{Integer. Number of Monte Carlo draws.}

\item{level}{Confidence level for the interval.}
}
\value{
A tibble with \code{angle_deg} and, if \code{se} is supplied, \code{lower},
\code{upper} and \code{level}.
}
\description{
For a projectile striking a flat surface, the angle of impact relative to
the surface is approximated by \code{asin(width / length)} of the resulting
elliptical defect (90 degrees is perpendicular). The relationship is
reliable for non-yielding surfaces (drywall, wood, sheet metal); it is
unreliable for glass, thick fabric, or when the defect is irregular.
Near 90 degrees the estimate is inherently imprecise because the ellipse
is almost circular; the Monte Carlo interval reflects that.
}
\examples{
impact_angle(width = 8, length = 16)          # 30 degrees
impact_angle(width = 8, length = 16, se = 0.5)
}
