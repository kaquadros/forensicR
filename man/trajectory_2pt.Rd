% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shooting.R
\name{trajectory_2pt}
\alias{trajectory_2pt}
\title{Trajectory through two points on the bullet path}
\usage{
trajectory_2pt(p1, p2, se_position = 0, id = "T1")
}
\arguments{
\item{p1, p2}{Numeric length-3 \code{c(x, y, z)}. \code{p1} is closer to the shooter,
\code{p2} further along the flight. The trajectory is anchored at \code{p2} (the
impact); \code{p1} is kept as \verb{$p1} and becomes the joint when the object is
used as a later segment of a \code{\link[=trajectory_path]{trajectory_path()}}.}

\item{se_position}{Standard error of each coordinate, scene units.}

\item{id}{Character label.}
}
\value{
An object of class \code{trajectory} with extra elements \code{length}
(distance between the two points) and \code{p1}.
}
\description{
Two defects (entry and exit of a wall, or defects in two surfaces), or a
defect and a probe tip. Positional uncertainty in each coordinate is
propagated to the direction.
}
\examples{
t <- trajectory_2pt(c(0, 0, 1.2), c(0, 2, 1.0), se_position = 0.01)
t$vertical_deg
}
