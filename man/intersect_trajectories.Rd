% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shooting.R
\name{intersect_trajectories}
\alias{intersect_trajectories}
\title{Convergence of two trajectories}
\usage{
intersect_trajectories(t1, t2, n_sim = 5000, level = 0.95)
}
\arguments{
\item{t1, t2}{\code{trajectory} objects.}

\item{n_sim}{Monte Carlo draws.}

\item{level}{Interval level.}
}
\value{
A list with \code{point} (midpoint of closest approach, central
estimate), \code{miss_distance} (central), \code{behind_both} (logical, central
estimate lies behind both defects), \code{summary} (tibble of Monte Carlo
quantiles for x, y, z and miss distance, plus \code{p_behind_both}) and
\code{samples} (tibble of per-draw results).
}
\description{
Finds the closest points between two back-projected trajectories. If the
shots came from one position, the lines should pass close to each other
\emph{behind} both defects. The miss distance relative to its Monte Carlo
spread tells you whether a common origin is supported; the midpoint
estimates that origin.
}
