% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elevation.R
\name{plot_wall_elevation}
\alias{plot_wall_elevation}
\title{Elevation view of one wall}
\usage{
plot_wall_elevation(
  wall,
  points = NULL,
  trajectories = NULL,
  walls = NULL,
  tol = 0.15,
  height = 2.5,
  door_height = 2,
  window_sill = 0.9,
  window_head = 2.1,
  units = "m",
  title = NULL,
  furniture = NULL,
  tol_furniture = 0.35,
  furniture_alpha = NULL
)
}
\arguments{
\item{wall}{Numeric length-4 \code{c(x1, y1, x2, y2)}; the first point appears
on the left. See \code{\link[=wall_from_room]{wall_from_room()}}.}

\item{points}{Optional scene points tibble with \code{z}.}

\item{trajectories}{Optional \code{trajectory} or list of them.}

\item{walls}{Optional walls tibble; door and window openings lying on
this wall are drawn.}

\item{tol}{Maximum perpendicular distance from the wall line for an item
to be shown, scene units.}

\item{height}{Wall (ceiling) height.}

\item{door_height, window_sill, window_head}{Heights used to draw openings.}

\item{units}{Axis unit label.}

\item{title}{Plot title. Default built from the wall end points.}

\item{furniture}{Optional \code{furniture} tibble. Objects whose footprint comes
within \code{tol_furniture} of the wall are drawn as semi-transparent boxes
with their height.}

\item{tol_furniture}{Distance from the wall within which objects are shown.}

\item{furniture_alpha}{Optional opacity overriding every object's own.}
}
\value{
A ggplot object.
}
\description{
The standard companion to the plan view: the wall drawn face-on with the
position and height of everything on or near it (bullet defects, stains,
openings). Items are selected by their perpendicular distance to the wall
line; heights come from the \code{z} column of the points. Trajectory anchors
are added as defects, with a short arrow showing the projected direction
of flight \emph{into} the wall.
}
