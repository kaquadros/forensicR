% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{render_scene_report}
\alias{render_scene_report}
\title{Render a scene report to Word, PDF and/or HTML}
\usage{
render_scene_report(
  input = NULL,
  output_dir,
  formats = c("docx", "pdf", "html"),
  params = list(),
  quiet = TRUE
)
}
\arguments{
\item{input}{Path to an \code{.Rmd} file. Defaults to a fresh copy of the
packaged \code{scene-report} template written to \code{output_dir} as
\code{scene-report.Rmd} (overwritten on every run). To customise the
template, copy it with \code{\link[=scene_report_skeleton]{scene_report_skeleton()}} under another name,
edit it, and pass that path as \code{input}.}

\item{output_dir}{Directory to write outputs to. Created if missing. There
is no default: choose the case folder explicitly, for example
\code{"case-2026-000123"}.}

\item{formats}{Character vector, any of \code{"docx"}, \code{"pdf"}, \code{"html"}.}

\item{params}{Named list passed to the document as \code{params}. The packaged
template understands: \code{case_id}, \code{agency}, \code{investigator}, \code{scene_date},
\code{units}, \code{narrative} (markdown text), \code{points} (tibble from the
\verb{coords_*()} functions), \code{shooting} (a list with optional \code{impact}, from
\code{\link[=impact_angle]{impact_angle()}}, and \code{trajectory}, from \code{\link[=trajectory_2pt]{trajectory_2pt()}}), \code{evidence}
(an \code{\link[=evidence_log]{evidence_log()}}) and \code{death_scene} (a \code{\link[=death_scene_record]{death_scene_record()}}).
Sections whose object is missing print a placeholder line.}

\item{quiet}{Logical. Suppress pandoc/knitr output.}
}
\value{
Invisibly, a named character vector of output file paths.
}
\description{
Renders an R Markdown scene report to one or more output formats. By
default all three formats are produced so the investigator can pick the
one their agency expects (Word for editing, PDF for submission, HTML for
quick review). Every output ends with \code{\link[=report_footer]{report_footer()}}.
}
\seealso{
\code{\link[=report_footer]{report_footer()}}, \code{\link[=scene_report_skeleton]{scene_report_skeleton()}}
}
