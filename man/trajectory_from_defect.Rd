% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shooting.R
\name{trajectory_from_defect}
\alias{trajectory_from_defect}
\title{Trajectory from a single defect's ellipse on a vertical surface}
\usage{
trajectory_from_defect(
  anchor,
  width,
  length,
  major_axis_deg,
  wall_azimuth_deg,
  came_from = c("above", "below", "left", "right"),
  se = 0.5,
  se_orientation = 5,
  id = "T1"
)
}
\arguments{
\item{anchor}{Numeric length-3 \code{c(x, y, z)} of the defect.}

\item{width, length}{Ellipse axes.}

\item{major_axis_deg}{Orientation of the major axis on the surface as seen
by an observer on the shooter's side facing the wall: 0 = vertical,
90 = horizontal, measured clockwise from 12 o'clock (range 0 to 180).}

\item{wall_azimuth_deg}{Azimuth of the wall's outward normal, i.e. the
direction the wall \emph{faces} (toward the shooter), degrees clockwise from
north. A wall on the north side of a room faces south: 180.}

\item{came_from}{Which end of the major axis the bullet came from, as
seen by that observer: \code{"above"}, \code{"below"}, \code{"left"} or \code{"right"}.}

\item{se}{Measurement SE of the ellipse axes.}

\item{se_orientation}{SE of \code{major_axis_deg}, degrees.}

\item{id}{Character label.}
}
\value{
An object of class \code{trajectory}.
}
\description{
Combines the impact angle from the ellipse (\code{\link[=impact_angle]{impact_angle()}}) with the
orientation of the ellipse's major axis on the surface and a
directionality indicator (lead-in mark, pinch point, or exit beveling)
to obtain a full 3D trajectory. Use only when a rod cannot be placed.
}
\examples{
# North wall, bullet came from above-right, 30 deg to the surface
trajectory_from_defect(c(2, 5, 1.4), width = 6, length = 12,
                       major_axis_deg = 45, wall_azimuth_deg = 180,
                       came_from = "above")
}
