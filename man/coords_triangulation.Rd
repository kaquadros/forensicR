% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene_mapping.R
\name{coords_triangulation}
\alias{coords_triangulation}
\title{Convert triangulation measurements to XY}
\usage{
coords_triangulation(
  d1,
  d2,
  p1,
  p2,
  side = c("left", "right"),
  id = NULL,
  z = NA_real_,
  type = "evidence"
)
}
\arguments{
\item{d1, d2}{Numeric. Distances from the item to reference points \code{p1}, \code{p2}.}

\item{p1, p2}{Numeric length-2. XY of the two reference points.}

\item{side}{\code{"left"} or \code{"right"} of the directed line \code{p1 -> p2}.}

\item{id}{Optional character vector of item labels.}

\item{z}{Optional numeric. Height above the floor of each item (e.g. a
bullet defect in a wall). Default \code{NA}, i.e. floor level / not measured.}

\item{type}{Optional character. Item type used by the 3D renderers:
\code{"evidence"} (default, numbered marker), \code{"defect"} (bullet defect,
dark disc on the wall) or \code{"bloodstain"} (schematic red disc).}
}
\value{
A tibble with columns \code{id}, \code{x}, \code{y}, \code{z}, \code{type}, \code{method}. Rows
whose distances cannot meet (no triangle) get \code{NA} coordinates with a
warning.
}
\description{
Locates each item from its distances to two fixed reference points.
Two mirror-image solutions exist; \code{side} picks the one to the left
(\code{"left"}, default) or right of the line from \code{p1} to \code{p2}.
}
\examples{
coords_triangulation(d1 = 3, d2 = 4, p1 = c(0, 0), p2 = c(5, 0))
}
