% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene_mapping.R
\name{coords_baseline}
\alias{coords_baseline}
\title{Convert baseline (rectangular coordinate) measurements to XY}
\usage{
coords_baseline(
  along,
  offset,
  origin = c(0, 0),
  end = c(1, 0),
  id = NULL,
  z = NA_real_,
  type = "evidence"
)
}
\arguments{
\item{along}{Numeric. Distance along the baseline from the origin.}

\item{offset}{Numeric. Perpendicular distance from the baseline. Positive
values are to the left when walking from the origin toward the end point.}

\item{origin}{Numeric length-2. XY of the baseline origin. Default \code{c(0, 0)}.}

\item{end}{Numeric length-2. XY of the baseline end point. Default \code{c(1, 0)}
(baseline runs along the positive x-axis).}

\item{id}{Optional character vector of item labels.}

\item{z}{Optional numeric. Height above the floor of each item (e.g. a
bullet defect in a wall). Default \code{NA}, i.e. floor level / not measured.}

\item{type}{Optional character. Item type used by the 3D renderers:
\code{"evidence"} (default, numbered marker), \code{"defect"} (bullet defect,
dark disc on the wall) or \code{"bloodstain"} (schematic red disc).}
}
\value{
A tibble with columns \code{id}, \code{x}, \code{y}, \code{z}, \code{type}, \code{method}.
}
\description{
The baseline method fixes a reference line between two known points
(e.g. two wall corners). Each item is located by its distance \emph{along}
the baseline from the origin and its perpendicular \emph{offset} from the
line (positive to the left of the direction of travel).
}
\examples{
coords_baseline(along = c(2.4, 5.1), offset = c(1.2, -0.8), id = c("A1", "A2"))
}
