% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scene_mapping.R
\name{coords_polar}
\alias{coords_polar}
\title{Convert polar (azimuth/distance) measurements to XY}
\usage{
coords_polar(
  distance,
  azimuth,
  station = c(0, 0),
  id = NULL,
  z = NA_real_,
  type = "evidence"
)
}
\arguments{
\item{distance}{Numeric. Horizontal distance from the station.}

\item{azimuth}{Numeric. Degrees clockwise from north.}

\item{station}{Numeric length-2. XY of the instrument. Default \code{c(0, 0)}.}

\item{id}{Optional character vector of item labels.}

\item{z}{Optional numeric. Height above the floor of each item (e.g. a
bullet defect in a wall). Default \code{NA}, i.e. floor level / not measured.}

\item{type}{Optional character. Item type used by the 3D renderers:
\code{"evidence"} (default, numbered marker), \code{"defect"} (bullet defect,
dark disc on the wall) or \code{"bloodstain"} (schematic red disc).}
}
\value{
A tibble with columns \code{id}, \code{x}, \code{y}, \code{z}, \code{type}, \code{method}.
}
\description{
For total-station or compass-and-tape work. Azimuths follow the survey
convention: degrees clockwise from north (north = 0, east = 90).
}
\examples{
coords_polar(distance = 10, azimuth = 90)   # 10 units due east
}
