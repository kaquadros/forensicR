% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/furniture.R
\name{along_wall}
\alias{along_wall}
\title{Place an object against a wall, as it is measured in the field}
\usage{
along_wall(walls, side, from, length, depth, id, type, ..., room_id = "room")
}
\arguments{
\item{walls}{Walls tibble from \code{\link[=room_rect]{room_rect()}}.}

\item{side}{\code{"north"}, \code{"south"}, \code{"east"} or \code{"west"}.}

\item{from}{Distance from the left end of the wall to the object's left edge.}

\item{length}{Extent along the wall.}

\item{depth}{Extent into the room.}

\item{id, type}{Passed to \code{\link[=furniture]{furniture()}}.}

\item{...}{Further arguments to \code{\link[=furniture]{furniture()}} (\code{height}, \code{colour}, ...).}

\item{room_id}{Group id of the room rectangle in \code{walls}.}
}
\value{
A one-row \code{furniture} tibble.
}
\description{
Position is given as the distance \code{from} the wall's left end (as seen
from inside the room, see \code{\link[=wall_from_room]{wall_from_room()}}) to the object's left edge,
its \code{length} along the wall and its \code{depth} into the room.
}
\examples{
walls <- room_rect(0, 0, 6, 5)
along_wall(walls, "north", from = 0.5, length = 2.1, depth = 0.9, id = "Sofa", type = "sofa")
}
