% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render3d.R
\name{render_scene_3d}
\alias{render_scene_3d}
\title{Render a 3D reconstruction of the scene (rayrender)}
\usage{
render_scene_3d(
  walls,
  points = NULL,
  trajectories = NULL,
  furniture = NULL,
  furniture_alpha = NULL,
  file,
  view = c("door", "corner", "overhead", "dollhouse"),
  lookfrom = NULL,
  lookat = NULL,
  fov = NULL,
  wall_height = 2.5,
  back = 3,
  cone = TRUE,
  grid = 1,
  grid_on = c("floor", "walls"),
  grid_strength = 0.6,
  grid_labels = TRUE,
  omit_wall = NULL,
  width = 1000,
  height = 750,
  samples = 128,
  ...
)
}
\arguments{
\item{walls}{Walls tibble; see \code{\link[=room_rect]{room_rect()}} and \code{\link[=opening]{opening()}}.}

\item{points}{Optional scene points tibble (\code{z} and \code{type} used).}

\item{trajectories}{Optional \code{trajectory} or list of them.}

\item{furniture}{Optional \code{furniture} tibble; see \code{\link[=furniture]{furniture()}}. Objects
are drawn as semi-transparent boxes (or cylinders) with a label on top.}

\item{furniture_alpha}{Optional opacity overriding every object's own.}

\item{file}{Output PNG path. No default: name the file explicitly, for
example inside the case folder.}

\item{view}{Camera preset: \code{"door"} (inside, at the door, eye level),
\code{"corner"}, \code{"overhead"}, or \code{"dollhouse"} (from outside, south wall
removed). Ignored if \code{lookfrom} is given.}

\item{lookfrom, lookat}{Optional explicit camera position and target in
scene coordinates \code{c(x, y, z)}.}

\item{fov}{Field of view in degrees. Default depends on \code{view}.}

\item{wall_height}{Ceiling height.}

\item{back}{Length of trajectory rods behind the defect.}

\item{cone}{Logical. Draw the uncertainty cone for each trajectory.}

\item{grid}{Grid spacing, scene units. \code{0} disables all grid lines.}

\item{grid_on}{Where to draw grid lines: any of \code{"floor"} and \code{"walls"}
(wall grids carry the z axis: verticals at each x/y tick and horizontals
at each height tick). Use \code{"none"} for no lines but keep labels.}

\item{grid_strength}{Number in 0 to 1 controlling how strongly grid lines
stand out (line darkness and thickness). Wall lines are always drawn
fainter than floor lines.}

\item{grid_labels}{Logical. Draw the numbers: x/y along the floor edges and
z at the start of each wall's horizontal grid line.}

\item{omit_wall}{Optional side (\code{"north"}, \code{"south"}, \code{"east"}, \code{"west"})
to leave out so an outside camera can see in.}

\item{width, height, samples}{Passed to \code{\link[rayrender:render_scene]{rayrender::render_scene()}}.}

\item{...}{Further arguments to \code{\link[rayrender:render_scene]{rayrender::render_scene()}}.}
}
\value{
The output file path, invisibly.
}
\description{
Builds a schematic, to-scale 3D model of the room from the same objects
used by the 2D plots and renders it with 'rayrender': walls with door and
window panels, a floor grid with numbered x/y axes, numbered evidence
markers, bullet defects at their measured height, schematic bloodstain
discs, trajectories as rods and their angular uncertainty as translucent
cones. Deliberately schematic: courtroom demonstratives must be accurate
and non-prejudicial, so nothing is rendered photo-realistically.
}
