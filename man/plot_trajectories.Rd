% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shooting.R
\name{plot_trajectories}
\alias{plot_trajectories}
\title{Plot trajectories in plan or elevation view with uncertainty}
\usage{
plot_trajectories(
  trajectories,
  view = c("plan", "elevation"),
  back = 8,
  n_draw = 150,
  points = NULL,
  walls = NULL,
  furniture = NULL,
  furniture_alpha = NULL,
  convergence = FALSE,
  heights = c(standing = 1.5, kneeling = 1, prone = 0.3),
  units = "m"
)
}
\arguments{
\item{trajectories}{A \code{trajectory}, a \code{trajectory_path}, or a list of
them. Path segments are drawn between their joints; assumed segments are
dashed and run forward from the joint.}

\item{view}{\code{"plan"} or \code{"elevation"}.}

\item{back}{Distance to project back, scene units.}

\item{n_draw}{Number of Monte Carlo lines to draw.}

\item{points}{Optional scene points tibble (from \verb{coords_*()}) to overlay
in plan view.}

\item{walls}{Optional wall/door/window polylines for plan view; see
\code{\link[=room_rect]{room_rect()}} and \code{\link[=opening]{opening()}}.}

\item{furniture}{Optional \code{furniture} tibble drawn in plan view.}

\item{furniture_alpha}{Optional opacity overriding every object's own.}

\item{convergence}{Plan view only. \code{TRUE} computes
\code{\link[=intersect_trajectories]{intersect_trajectories()}} for every pair and draws the Monte Carlo
cloud of closest-approach points (only draws lying behind both defects)
with the central estimate marked. Alternatively pass one result of
\code{\link[=intersect_trajectories]{intersect_trajectories()}} or a list of them. \code{FALSE} draws nothing.}

\item{heights}{Muzzle heights to mark in elevation view.}

\item{units}{Axis unit label.}
}
\value{
A ggplot object.
}
\description{
Plan view: bird's-eye XY with each trajectory drawn back from its defect
by \code{back} units; faint lines are Monte Carlo draws and show the cone of
uncertainty. Elevation view: height against horizontal distance back from
the defect, one panel per trajectory, with assumed muzzle heights dashed.
}
