# SHA-256 hash of files

SHA-256 hash of files

## Usage

``` r
file_hash(paths)
```

## Arguments

  - paths:
    
    Character vector of file paths.

## Value

Named character vector of hex digests.
