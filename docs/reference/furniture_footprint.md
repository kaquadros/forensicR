# Footprint polygons of scene objects

Footprint polygons of scene objects

## Usage

``` r
furniture_footprint(f)
```

## Arguments

  - f:
    
    A `furniture` tibble.

## Value

A tibble with columns `id`, `x`, `y`, `type`, `colour`, `alpha`,
`measured`, one row per vertex.
