# Catalogue of object types with default height, colour and label

Used by `furniture()` and friends to fill in defaults. Heights are
typical values and should be replaced by measurements whenever
available.

## Usage

``` r
furniture_catalogue()
```

## Value

A tibble with columns `type`, `height`, `colour`, `label`.
